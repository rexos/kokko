alert("Messaggio segnato come \"letto\"");
$('#label-<%= escape_javascript(@message_id.to_s) %>').fadeOut('fast');
var msg_num = parseInt($('#msg_notification').text().substr(0,2));
    if(msg_num - 1 == 0){
    	       $('#msg_notification').fadeOut('fast');
    }
    else{
	var text = " messaggi";
	if(msg_num - 1 == 1){
		   text = " messaggio"
	}
	$('#msg_notification').text(msg_num-1+text);
    }
