<div class="well">
  <div class="center">
    <h1><%=@lesson.title%></h1>
  </div>
  <% @lesson.exercises.each do |e| %>
  <div class="well" style="background-color:white">
    <div class="title">
      <h5><%= e.name %></h5>
    </div>
    <div class="body" style="display:none">
      <p><%= e.description %></p>
      <p><%= e.difficulty %></p>
      <p><%= e.video_url %></p>
      <p><%= @lesson.associations.find_by_exercise_id(e.id).repetitions %></p>
      <%=link_to '<label class="label label-info">Edit</label>'.html_safe, action: :index, :id => @lesson.id %>
      <%=link_to '<label class="label label-info">Remove</label>'.html_safe,  action: :index, :id => @lesson.id %>
    </div>
  </div>
  <% end %><br />
  <button href="#myModal" class="btn btn-primary btn-default" data-toggle="modal"></i class="icon-plus icon-white"></i>Add Exercise</button>
</div>


<%= render "modal" %>
