<div class="page-title">
    <h2 class="center">
        <i class="icon-group"></i>&nbsp;Sala d'attesa
    </h2>
  </div>

<div class="row-fluid">
	<div class="span12">
			<div style="margin-left: 15px"><h3>Utenti iscritti al programma <strong><%= @program.title.capitalize %></strong></h3></div>
		<% if @attending.count == 0 %>
		<div class="alert alert-danger" style="margin-top: 20px">
        <h3 class="center" >Non è iscritto nessuno al momento!</h3>
      	</div>
      	<% end %>
		<% @attending.each do |u| %>
		<div class="user-box">
		  <%= gravatar_for(User.find(u.id), "avatar") %>
		  <h4 class="center"><%= u.username.capitalize %>&nbsp;
		    <% if u.online %>
		    <icon class="icon-circle" style="color:#2ecc71"></i>
                    <% else %>
		    <icon class="icon-circle"></i>
                    <% end %>
		  </h4>
		</div>
		<% end %>
	</div>
</div>
<br />
<div class="row-fluid">
  <div class="span12 center alert alert-info lesson-ready">
    <h2>La <%= @event.title %> inizia il : <%= @event.start.to_s[0..9] %> alle <%= @event.start.to_s[10...-7] %> </h2>
    <p>
    </p>    
  </div>
</div>
