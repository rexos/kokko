<div class="well">
  <div class="center">
    <h2>List of all Programs</h2>
  </div>

  <% if @programs.count == 0 %>
    <div class="alert alert-error">
      <h2><strong><center>There isn't any active Program right now</center></strong></h2>
    </div>

  <% else %>
<<<<<<< HEAD
  <ul type="none" class="programs">
    <% @programs.each do |program| %>
    <li>
      <div class="well" style="background-color:white">
	<div class="title">
	  <h4><%= program.title %></h4>
	</div>
	<p><%= program.description %></p>
	<h5>Lessons of <%= program.title %> :</h5>
	<ul>
	  <% program.lessons.each do |l| %>
	  <li>
	    <%=link_to l.title, controller: :lessons, action: :index, :id => l.id %>
	  </li>
	  <% end %>
	</ul>
	<%= link_to 'Remove', action: :destroy, :id => program.id %>
	<%= link_to 'Add Lesson', controller: :lessons, action: :new, :prog_id => program.id %>
      </div>
    </li>
    <% end %>
  </ul>
=======
    <ul type="none" class="programs">
      <% @programs.each do |program| %>
      <li>
        <div class="well" style="background-color:white">
          <div class="title">
	          <h4><%= program.title %></h4>
          </div>
          <p><%= program.description %></p>
          <h5>Lesssons of <%= program.title %> :</h5>
          <ul>
	          <% program.lessons.each do |l| %>
            <li>
              <%= l.title %>
	          </li>
            <% end %>
          </ul>
          <%= link_to 'Remove', action: :destroy, :id => program.id %>
          <%= link_to 'Add Lesson', controller: :lessons, action: :new, :prog_id => program.id %>
        </div>
      </li>
      <% end %>
    </ul>
>>>>>>> ec7883d180a55885226663d8c2fb00ee839e8c12
  <% end %>

  <div class="center">
    <button href="#myModal" class="btn btn-primary btn-default" data-toggle="modal"></i class="icon-plus icon-white"></i>Add One</button>
  </div>
</div>

<%= render "modal" %>
