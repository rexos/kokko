$('div.modal-body').prepend(
	'<div class="alert alert-error"><strong>Attenzione! Sono stati rilevati i seguenti errori durante la registrazione:</strong><br /> <p><%= escape_javascript(@user.errors.full_messages.to_s)%></p></div>'
	);
$('div.alert-error').fadeOut(7000);
